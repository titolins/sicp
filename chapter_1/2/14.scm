;; 1.14
;; space required -> O(a) linear to amount (max height of the tree), "determined by the number of times the smallest denomination divides into the amount"
;; operations / time -> O(a^k) where k = kinds-of-coins
