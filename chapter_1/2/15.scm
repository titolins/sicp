;; 1.15
;; a. p will be called five times
;; b.
;; sine will divide the angle by 3 on every iteration until the angle is small enough
;; counting the number of times p gets applied, we can verify the both the order of growth in space
;; and number os steps is logarithmic.
